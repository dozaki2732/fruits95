import React from "react";

class totalIncome extends React.Component {
  render() {
    return (
      <>
        {/* fitler the data into an array, add everything in that array. use 2 filters. 
            
            
            */}
      </>
    );
  }
}

export default totalIncome;
